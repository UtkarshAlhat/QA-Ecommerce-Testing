Playwright + pytest automation for AutomationExercise.

Run:
1. Activate venv: source venv/bin/activate
2. Install deps: pip install -r requirements.txt
3. Install browsers: python -m playwright install --with-deps
4. Run tests: pytest Automation/tests -q --html=Automation/reports/report.html
